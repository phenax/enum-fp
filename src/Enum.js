
import { createEnumConstructor } from './utils';
import createConstructor from './createConstructor';

// Enum :: Array String | Object * -> Enum
export default createEnumConstructor({ createConstructor });
